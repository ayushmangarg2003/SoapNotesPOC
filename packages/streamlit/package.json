{
  "name": "text_organizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "streamlit/.venv/bin/python app.py",
    "dev-ui":"streamlit/.venv/bin/python -m streamlit run app.py",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-dependencies": "python -m venv streamlit/.venv && streamlit/.venv/bin/pip install -r requirements.txt"

  },
  "author": "",
  "license": "ISC"
}
